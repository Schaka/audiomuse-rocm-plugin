[pytest]
testpaths = tests
# The plugin is imported as `plugin.rocm_accelerator`, matching how core loads
# it, so the directory holding this one has to be importable - not this one.
pythonpath = ..
addopts = --strict-markers
